export interface Settings {
  scrollSpeed: number;
  fontSize: number;
  lineHeight: number;
  isMirrored: boolean;
  fontFamily: string;
  showGuides: boolean;
  textAlign: 'left' | 'center' | 'right';
  showCamera: boolean;
  fontColor: string;
  textBackgroundColor: string;
}
